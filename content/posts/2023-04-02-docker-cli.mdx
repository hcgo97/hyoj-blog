---
title: docker cli
slug: "/docker-cli"
date: 2023-04-02
---

# docker cli

Dockerfile

###

docker image build -t :

- -t :

###

docker image build -t /:

###

docker search [] \<\>

###

docker pull [] :

### shell

docker container run -t -p 9000:8080 :

- : 9000 9000 8080 ( : )
- -d:
- -e:
- --name:
- -v: volumnes ( DIR:DIR)

### docker-compose.yml

docker -f docker-compose.yml -t up

- -f: compose
- -t:

### docker compose up / down / run

- down , db, redis, sidekiq, api
- run up( )
- IDE api , db, redis, sidekiq

###

docker exec -it local-mysql bash

- -it: interactive terminal

### docker mysql

mysql -u root -p -h 127.0.0.1

###

docker image rm $(docker image ls -a --filter "dangling=true" -q)

docker compose -f docker-compose.local.yml exec redis /bin/sh

### status = exited

docker container rm $(docker container ls -a --filter "status=exited" -q)

```bash
cd warning -\> lena.local-db-set
docker compose -f docker-compose.yml -p lena.local-db-set up
```

###

runlike -p

runlike -p link-talk-server-api_db_1

###

```bash
# tmp_container   /root/data/test.md   ~/data
docker cp tmp_container:/root/data/test.md ~/data/
```

###

```bash
#  ~/data/test.md  tmp_container   /root/data/
docker cp ~/data/test.md tmp_container:/root/data/
```

�테이너 내부에 접속하기

docker exec -it local-mysql bash

- -it: interactive terminal 모드

### docker 로 띄운 mysql 에 접속하기

mysql -u root -p -h 127.0.0.1

### 연관 관계 아예 없는 사용하지 않는 이미지 삭제

docker image rm $(docker image ls -a --filter "dangling=true" -q)

docker compose -f docker-compose.local.yml exec redis /bin/sh

### status = exited 인 컨테이너만 삭제

docker container rm $(docker container ls -a --filter "status=exited" -q)

```bash
cd warning -\> lena.local-db-set
docker compose -f docker-compose.yml -p lena.local-db-set up
```

### 현재 실행중인 컨테이너 옵션 확인

runlike -p 컨테이너 명

runlike -p link-talk-server-api_db_1

### 도커 컨테이너 → 로컬로 파일 복사

```bash
# tmp_container 컨테이너 내부의 /root/data/test.md 파일을 로컬의 ~/data 로 복사하기
docker cp tmp_container:/root/data/test.md ~/data/
```

### 로컬 → 도커 컨테이너로 파일 복사

```bash
# 로컬의 ~/data/test.md 파일을 tmp_container 컨테이너 내부의 /root/data/ 로 복사하기
docker cp ~/data/test.md tmp_container:/root/data/
```
