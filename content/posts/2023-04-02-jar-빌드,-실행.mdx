---
title: jar 빌드, 실행
slug: "/jar-빌드,-실행"
date: 2023-04-02
---

# jar ,

# bootJar

- Spring Boot Plugin Gradle .

```
plugins \{
	id 'org.springframework.boot' version '2.5.1'
\}
```

- `bootJar` jar , .
- jar `bootRepackage` Spring Boot 2.0`bootJar` .

##

- ` build.gradle``bootJar ` .
- `mainClass` ,`archieveName` jar .

```
bootJar \{
	mainClass = 'com.example.ExampleApplication'
\}
```

##

     `./build/libs``jar` .

```bash
$ ./gradlew bootJar

# clean: /build
$ ./gradlew clean bootJar
```

##

`jar` `java -jar` , .

```bash
$ ./gradlew bootRun

# clean: /build
$ ./gradlew clean bootRun

# nohup:
# & :
$ nohup java -Dspring.profiles.active=dev -jar pupsload-0.0.1-SNAPSHOT.jar &
```

: [https://velog.io/@jwkim/spring-boot-build-jar](https://velog.io/@jwkim/spring-boot-build-jar)

우고 빌드
$ ./gradlew clean bootJar

````

## 실행

직접 `jar` 파일이 있는 곳에서 `java -jar`으로 실행해도 되고, 루트 디렉토리에서 아래 명령어를 실행해도 된다.

```bash
$ ./gradlew bootRun

# clean: /build 폴더 다 지우고 실행
$ ./gradlew clean bootRun

# nohup: 터미널이 끊겨도 실행한 프로세스는 계속 동작하도록 하기
# & : 백그라운드에서 실행하기
$ nohup java -Dspring.profiles.active=dev -jar pupsload-0.0.1-SNAPSHOT.jar &
````

출처: [https://velog.io/@jwkim/spring-boot-build-jar](https://velog.io/@jwkim/spring-boot-build-jar)
